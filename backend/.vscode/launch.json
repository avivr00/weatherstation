{
  "version": "0.2.0",
  "configurations": [
    {
        "name": "Python: pytest current file",
        "type": "debugpy",
        "request": "launch",
        "program": "${workspaceFolder}/venv/bin/pytest",
        "args": ["${file}"],
        "console": "integratedTerminal",
        "cwd": "${workspaceFolder}"
      }
  ]
}
