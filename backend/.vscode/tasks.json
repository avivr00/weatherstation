{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run tests (pytest)",
      "type": "shell",
      "command": "${workspaceFolder}/venv/bin/python -m pytest -q",
      "group": "test",
      "presentation": {
        "reveal": "always"
      }
    }
  ]
}
